{% extends 'base.html.twig' %}

{% block title %}PlayerDrill{% endblock %}

{% block body %}
    <h1>PlayerDrill</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ player_drill.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('player_drill_index') }}">back to list</a>

    <a href="{{ path('player_drill_edit', {'id': player_drill.id}) }}">edit</a>

    {{ include('player_drill/_delete_form.html.twig') }}
{% endblock %}
